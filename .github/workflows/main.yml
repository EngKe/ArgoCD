name: Repository Dispatch

on:
  repository_dispatch:
    types: [new-image]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    
    - name: clone repo
      run: |
        git config --global user.email "kesginengin@gmail.com"
        git config --global user.name "EngKe"
        git clone https://github.com/EngKe/ArgoCD.git
        cd ArgoCD
        git checkout dev
        
    - name: Update Image Version
      id: imgupd
      uses: mikefarah/yq@master
      with:
        cmd: yq eval '.spec.template.spec.containers[0].image = "${{ github.event.client_payload.image }}"' -i pythonapp/deployment.yaml 
    
    - name: open pr
      run: |
        git config --global user.email "kesginengin@gmail.com"
        git config --global user.name "EngKe"
        git add .
        git commit -m "Apply image name changes"
        git status
        git push
        git request-pull dev origin main

